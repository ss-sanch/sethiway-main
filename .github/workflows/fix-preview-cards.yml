name: Fix preview cards
on:
  push:
    branches:
      - feature/backtest-v2-polish
    paths:
      - scripts/fix_preview_cards.py
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/backtest-v2-polish
      - run: python scripts/fix_preview_cards.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sethiquant.html
          git diff --cached --quiet && exit 0
          git commit -m "Polish pre-run analysis previews [skip ci]"
          git push origin HEAD:feature/backtest-v2-polish
