name: Phase 1 Integration Runner

on:
  push:
    branches: [phase1-integration]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Runner check
        run: echo "phase1-runner-ok"
