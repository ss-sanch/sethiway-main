name: Run Backtest V2 polish migration
on:
  push:
    branches:
      - feature/backtest-v2-polish
    paths:
      - scripts/apply_backtest_v2_polish.py
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/backtest-v2-polish
      - run: python scripts/apply_backtest_v2_polish.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sethiquant.html
          git diff --cached --quiet && exit 0
          git commit -m "Polish Backtest V2 previews [skip ci]"
          git push origin HEAD:feature/backtest-v2-polish
