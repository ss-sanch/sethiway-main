name: Run MRL derivatives migration
on:
  push:
    branches:
      - feature/mrl-derivatives-risk
    paths:
      - scripts/apply_mrl_derivatives_risk.py
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/mrl-derivatives-risk
      - run: python scripts/apply_mrl_derivatives_risk.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sethiquant.html
          git diff --cached --quiet && exit 0
          git commit -m "Add Market Risk Lab derivatives overlay [skip ci]"
          git push origin HEAD:feature/mrl-derivatives-risk
